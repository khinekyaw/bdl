import React from "react"

const LocalPlayerList = () => {
  return (
    <div className="grid grid-cols-3 gap-3">
      {/* {players.map((player, idx) => (
        <PlayerCard key={player.id} {...player} />
      ))} */}
    </div>
  )
}

export default LocalPlayerList
